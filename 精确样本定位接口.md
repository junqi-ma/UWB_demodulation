# DW1000 全文件扫描与精确样本定位接口

运行：

```matlab
run_decode_uwb_all
```

扫描采用三级流程：

1. 每 100 个原始复采样读取一个完整 IQ 记录，形成能量包络和候选区间。
2. 只在能量区间内以 32 倍抽取执行重复前导相关。
3. 每个候选区间单独按原始采样率解调，避免解调器跳到相邻的更强包。

## 样本坐标约定

所有 `abs_*_sample` 和区间矩阵都使用：

- 原始 `.dat` 文件的复采样编号；
- **零基索引**；
- 区间终点为**闭区间**。

因此，文件区间 `[firstSample, lastSample]` 对应的样本数为：

```matlab
sampleCount = lastSample - firstSample + 1;
```

如果已经把相同区间读入 MATLAB 数组，文件样本 `n` 对应数组下标：

```matlab
matlabIndex = n - firstSample + 1;
```

## 每个包的定位字段

`results.frames(k)` 包含：

| 字段 | 含义 |
|---|---|
| `abs_start_sample` | 精细同步得到的第一个 SYNC 参考采样 |
| `abs_end_sample` | 已解码 payload 的最后一个软码片对应采样；PHR 无效时使用回退估计 |
| `abs_phr_start_sample` / `abs_phr_end_sample` | PHR 起止采样 |
| `abs_payload_start_sample` / `abs_payload_end_sample` | payload 起止采样 |
| `localization_start_sample` / `localization_end_sample` | 含保护区的信号处理区间 |
| `has_precise_end_sample` | 是否由有效 PHR/payload 长度获得精确终点 |
| `end_sample_source` | 终点来自 payload、PHR，还是回退估计 |

重复从不同窗口解调同一实测包时，精细起点保持在约一个 X410
采样的舍入误差内。下游对齐和抵消仍可在
`abs_start_sample` 附近做很小范围的相关搜索。

## 可直接使用的区间矩阵

```matlab
saved = load('all_frames_cir.mat', 'results');
results = saved.results;

% 每个已保存包的同步起点到帧终点。
packetIntervals = results.packet_intervals;

% 只含具有精确 payload 终点的包。
precisePacketIntervals = results.precise_packet_intervals;

% 含前后保护区，可直接交给时域置零或其他解调器的 blank_intervals。
blankIntervals = results.precise_blank_intervals;
```

例如，在其他解调代码中屏蔽已经定位的 DW1000 包：

```matlab
options.blank_intervals = results.precise_blank_intervals;
options.blank_taper_samples = 256;
options.blank_weight = 0;
```

执行波形重建和干扰抵消时，应使用：

```matlab
nominalStart = results.frames(k).abs_start_sample;
```

作为复制波形的零基名义起点，然后像
`run_cancel_all_uwb_packets.m` 一样在该位置附近进行小范围精对齐。
不要使用能量区间起点或第二级相关候选作为最终抵消起点。

